# Core web framework
Flask==3.0.3
flask-cors==4.0.0
gunicorn==21.2.0

# ML / LLM - pinned to avoid pip backtracking
torch==2.2.1
transformers==4.41.2
accelerate==0.30.1
sentence-transformers==2.7.0
tokenizers==0.19.1
sentencepiece==0.2.0

# huggingface-hub: must be >=0.23.0 (transformers 4.41.2 needs it)
# NOT pinned to 0.23.4 - let pip pick compatible version in >=0.23.0,<0.24.0
huggingface-hub>=0.23.0,<0.24.0

# LlamaIndex packages must stay on the same 0.10.x line. Older core builds miss
# symbols imported by the HuggingFace integration, and loose ranges drift apart.
llama-index==0.10.68
llama-index-core==0.10.68
llama-index-llms-huggingface==0.2.8
llama-index-embeddings-huggingface==0.2.3
llama-index-vector-stores-chroma==0.1.10

# Vector DB
chromadb==0.4.24

# PDF parsing
PyMuPDF==1.23.26

# LangChain (used by some llama-index integrations)
langchain-community==0.0.24

# Logstash logging
python-logstash-async==2.5.0
